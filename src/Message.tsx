// PascalCasing is used for Naming convention
function Message() {
    // The below code is called JSX: Javascript XML, the below code will be compiled to Javascript.
    // Visit: babeljs.io/repl to see how the code is converted to React code.
    const name = "Sachin";
    return <h1> Hello {name}. </h1>;
}

// TO use the code
export default Message;